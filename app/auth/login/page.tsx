"use client"; import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { useState, FormEvent } from 'react'; import { createClient } from '@/lib/supabase/client'; import { Button } from '@/components/ui/button'; import { Input } from '@/components/ui/input'; import { Label } from '@/components/ui/label'; import { Scissors, ArrowLeft, Loader2 } from 'lucide-react'; export default function LoginPage() { const router = useRouter(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(null); const [loading, setLoading] = useState(false); async function handleSubmit(e) { e.preventDefault(); setError(null); setLoading(true); try { const supabase = createClient(); if (!supabase) { setError('Erro de configuracao'); return; } const { error: signInError } = await supabase.auth.signInWithPassword({ email, password, }); if (signInError) { setError('Email ou senha incorretos'); return; } router.push('/dashboard'); router.refresh(); } catch (err) { setError('Erro inesperado'); } finally { setLoading(false); } } return ( <div className='min-hscreen flex items-center justify-center p-8'> <div className='w-full max-w-md'> <div className='flex items-center gap-2 mb-8'> <Scissors className='w-6 h-6 text-primary' /> <span className='text-xl font-bold'>FADEZONE</span> </div> <form onSubmit={handleSubmit} className='space-y-6'> {error && <div className='p-4 bg-destructive/10 text-destructive text-sm rounded-lg'>{error}</div>} <div className='space-y-2'> <Label>Email</Label> <Input type='email' value={email} onChange={(e) => setEmail(e.target.value)} required /> </div> <div className='space-y-2'> <Label>Senha</Label> <Input type='password' value={password} onChange={(e) => setPassword(e.target.value)} required /> </div> <Button type='submit' className='w-full' disabled={loading}> {loading ? 'Entrando...' : 'Entrar'} </Button> </form> <p className='mt-8 text-center text-sm'> Nao tem uma conta? <Link href='/auth/sign-up' className='text-Aɥ�����ɥ�ȁ���ф�1���������𽑥���𽑥������