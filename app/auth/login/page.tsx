import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { useState } from 'react'; import { createClient } from '@/lib/supabase/client'; import { Button } from '@/components/ui/button'; import { Input } from '@/components/ui/input'; import { Label } from '@/components/ui/label'; import { Scissors, ArrowLeft, Loader2 } from 'lucide-react'; export default function LoginPage() { const router = useRouter(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(null); const [loading, setLoading] = useState(false); async function handleSubmit(e) { e.preventDefault(); setError(null); setLoading(true); try { const supabase = createClient(); if (!supabase) { setError('Erro de configuração: Variáveis do Supabase não encontradas.'); return; } const { error: signInError } = await supabase.auth.signInWithPassword({ email, password, }); if (signInError) { setError('Email ou senha incorretos. Tente novamente.'); return; } router.push('/dashboard'); router.refresh(); } catch (err) { console.error('Login error:', err); setError('Ocorreu um erro inesperado.'); } finally { setLoading(false); } } return ( <div className='min-h-screen flex'> <div className='flex-1 flex items-center justify-center p-8'> <div className='w-full max-w-md'> <Link href='/' className='inline-flex items-center gap-2 text-muted-foreground hover:text-primary transition-colors mb-8' > <ArrowLeft className='w-4 h-4' /> Voltar para home </Link> <div className='flex items-center gap-2 mb-8'> <div className='w-10 h-10 bg-primary rounded-lg flex items-center justify-center'> <Scissors className='w-6 h-6 text-primary-foreground' /> </div> <span className='text-xl font-bold tracking-tight'> FADE<span className='text-primary'>ZONE</span> </span> </div> <div className='mb-8'> <h1 className='text-3xl font-bold mb-2'>Bem-vindo de volta</h1> <p className='text-muted-foreground'> Entre na sua conta para agendar horarios e acompanhar seus cortes. </p> </div> <form onSubmit={handleSubmit} className='space-y-6'> {error && ( <div className='p-4 bg-destructive/10 border border-destructive/30 rounded-lg text-destructive text-sm'> {error} </div> )} <div className='space-y-2'> <Label htmlFor='email'>Email</Label> <Input id='email' type='email' placeholder='seu@email.com' value={email} onChange={(e) => setEmail(e.target.value)} required className='h-12' /> </div> <div className='space-y-2'> <div className='flex items-center justify-between'> <Label htmlFor='password'>Senha</Label> <Link href='/auth/forgot-password' className='text-sm text-primary hover:underline'> Esqueceu a senha? </Link> </div> <Input id='password' type='password' placeholder='••••••••' value={password} onChange={(e) => setPassword(e.target.value)} required className='h-12' /> </div> <Button type='submit' className='w-full h-12 neon-border' disabled={loading}> {loading ? ( <> <Loader2 className='w-4 h-4 mr-2 animate-spin' /> Entrando... </> ) : ( 'Entrar' )} </Button> </form> <p className='mt-8 text-center text-muted-foreground'> Nao tem uma conta?{' '} <Link href='/auth/sign-up' className='text-primary hover:underline font-medium'> Criar conta </Link> </p> </div> </div> <div className='hidden lg:flex flex-1 bg-card items-center justify-center relative overflow-hidden'> <div className='absolute inset-0 urban-texture' /> <div className='absolute top-1/4 -right-32 w-96 h-96 bg-primary/20 rounded-full blur-3xl' /> <div className='absolute bottom-1/4 -left-32 w-96 h-96 bg-accent/20 rounded-full blur-3xl' /> <div className='relative z-10 text-center p-12'> <h2 className='text-4xl font-bold mb-4'> <span className='neon-glow text-primary'>ESTILO</span> <br /> <span className='text-foreground'>COMEÇA AQUI</span> </h2> <p className='text-muted-foreground max-w-sm mx-auto'> Agende seu proximo corte em segundos e garanta o melhor atendimento. </p> </div> </div> </div> ); }
