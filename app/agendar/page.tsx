import { createClient } from '@/lib/supabase/server'; import { redirect } from 'next/navigation'; import { BookingFlow } from '@/components/booking/booking-flow'; import Link from 'next/link'; import { ArrowLeft, Scissors } from 'lucide-react'; export const dynamic = 'force-dynamic'; export default async function AgendarPage() { const supabase = await createClient(); if (!supabase) { redirect('/auth/login?next=/agendar'); } const { data: { user } } = await supabase.auth.getUser(); if (!user) { redirect('/auth/login?next=/agendar'); } const { data: services } = await supabase.from('services').select('*').eq('is_active', true).order('price_cents', { ascending: true }); const { data: barbers } = await supabase.from('barbers').select('*').eq('is_active', true); return ( <div className='min-h-screen bg-background'> <header className='border-b border-border bg-card/50 backdrop-blur-md sticky top-0 z-50'> <div className='max-w-4xl mx-auto px-4 py-4 flex items-center justify-between'> <Link href='/' className='flex items-center gap-2'> <div className='w-8 h-8 bg-primary rounded-lg flex items-center justify-center'> <Scissors className='w-4 h-4 text-primary-foreground' /> </div> <span className='font-bold tracking-tight'> FADE<span className='text-primary'>ZONE</span> </span> </Link> <Link href='/' className='flex items-center gap-2 text-sm text-muted-foreground hover:text-primary transition-colors' > <ArrowLeft className='w-4 h-4' /> Voltar </Link> </div> </header> <main className='max-w-4xl mx-auto px-4 py-8'> <div className='mb-8'> <h1 className='text-3xl font-bold mb-2'>Agendar Horario</h1> <p className='text-muted-foreground'> Escolha o servico, barbeiro e horario de sua preferencia. </p> </div> <BookingFlow services={services || []} barbers={barbers || []} userId={user.id} /> </main> </div> ); }
